import './style.scss'

const StartAndStop = () => {
    return (
        <div className={'StartAndStopBody'}>
            <button>Start</button>
            <button>Stop</button>
        </div>
    )
}

export default StartAndStop